from django.urls import path, include

# from proxy_app.views import ProxyListView

app_name = "proxy"

# urlpatterns = [
#     path("list/", ProxyListView.as_view(), name="list"),
#     # path("add/", SignUpView.as_view(), name="signup"),
#     # path("details/", SignUpView.as_view(), name="signup"),
#     # path("delete/", SignUpView.as_view(), name="signup"),
# ]
